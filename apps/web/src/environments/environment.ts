import { Environment } from '@portfolio/web-shared';

export const environment: Environment = {
  production: true,
  apiUrl: 'https://your-production-domain.com/api/v1',
  apiVersion: 'v1',
  authCookieDomain: 'your-production-domain.com',
  logLevel: 'error',
};
